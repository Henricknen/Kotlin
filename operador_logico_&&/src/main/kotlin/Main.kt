fun main(args: Array<String>) {

    if(20 == 20 && 50 > 20 && 10 < 20) {        // operador lógico '&&' equivala a 'e', se os dois lados não forem 'verdadeiros' o retorno será 'falso'
        println("verdadeiro")
    } else {
        println("falso")
    }
}